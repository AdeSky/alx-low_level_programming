#include <stdio.h>

/**
 * main -prints all single digit numbers of base 10 starting from 0
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;
	int a;

	a = n % 10

	for (n = 0; n < 10; n++)
		putchar(a + '0');
	putchar('\n');
	return (0);
}
