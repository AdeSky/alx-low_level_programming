#include "main.h"

/** add - a function that adds two integers and returns the result
 * @b: number input
 * @f: number input
 * Return: the result
 */
int add(int b, int f)
{
	int plus;
	plus = (b + f);

	_putchar(plus)
	return (plus);
}
